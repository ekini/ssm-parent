# .goreleaser.yml
version: 2

# release-please creates the release and its notes; only add artifacts to it
release:
  mode: keep-existing

builds:
  - binary: ssm-parent
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
